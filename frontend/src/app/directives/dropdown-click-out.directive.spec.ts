import { DropdownClickOutDirective } from './dropdown-click-out.directive';

describe('DropdownClickOutDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownClickOutDirective();
    expect(directive).toBeTruthy();
  });
});
